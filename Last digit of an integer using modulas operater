#include <stdio.h>

int main() {
int integer,last_digit;
printf("enter a integer:");
scanf("%d", &integer);
last_digit = integer % 10;
printf(" last_digit of %d is %d\n", integer, last_digit);
    return 0;
}
